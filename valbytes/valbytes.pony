"""
Immutable byte sequences with efficient concatenation
and slicing.

Use `ByteArrays` for the tree-backed implementation;
use `ValBytes` as the common interface that also
covers `Array[U8] val`.
"""
